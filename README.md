pip install -r requirement.txt

To reproduce experiment results, update the configs in MultiResDriftConfig class in train_nystrom_cond_control_variate.ipynb change the out_dir to match where you want to keep the experiment results.
